import React from 'react'
const Input = (props) => {
  return (
    <div className='inputs'>
        <input type="text" placeholder={props.name} className="inputag"/>
    </div>
  )
}

export default Input